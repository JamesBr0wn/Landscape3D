<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Landscape3D" parent="Theme.Material3.Dark.NoActionBar">
        <!-- parent="Theme.Material3.Dark.NoActionBar"> (works, dark) -->
        <!-- parent="Theme.AppCompat.NoActionBar" (works, smaller fonts, dark actionbar) -->
        <!-- parent="Theme.AppCompat.DayNight.NoActionBar" (works, smaller fonts, light) -->
        <!-- parent="Theme.Material3.DayNight.NoActionBar" (original generated)-->
        <!-- parent="Theme.Material3.DynamicColors.Dark.NoActionBar" (more colorful buttons) -->
        <!-- parent="Theme.Material3Expressive.DynamicColors.Dark.NoActionBar" (more colorful buttons) -->
    </style>

    <style name="Theme.Landscape3D" parent="Base.Theme.Landscape3D" />
</resources>